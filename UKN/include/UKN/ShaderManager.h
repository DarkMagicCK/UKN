#include "mist/Platform.h"
#include "mist/Singleton.h"

namespace ukn {

    class ShaderManager: public mist::Singleton<ShaderManager> {
    protected:
        ShaderManager();
        virtual ~ShaderManager();

    public:

    private:

    };


} // namespace ukn